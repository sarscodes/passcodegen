const { alpha, symbols } = require('./characters.json');

const generateRandom = () => {
  return {
    number: () => {},
    lowercase: () => {},
    uppercase: () => {},
    symbol: () => {},
  };
};

module.exports = generateRandom();
